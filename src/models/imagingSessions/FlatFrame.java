package models.imagingSessions;

public class FlatFrame extends ImagingFrame {
    public FlatFrame() {}

    public FlatFrame(String date, Integer totalSubs, Double subLength) {
        super(date, totalSubs, subLength);
    }
}
